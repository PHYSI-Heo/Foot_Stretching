<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/menu_btn_frame"
    android:background="@drawable/bg_menu_btn"
    xmlns:tools="http://schemas.android.com/tools">

    <TextView
        android:id="@+id/tv_point_lien"
        android:layout_width="match_parent"
        android:layout_height="12dp"
        tools:background="@color/colorAccent"
        android:layout_alignParentBottom="true"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/tv_point_lien"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/iv_menu_icon"
            android:layout_width="match_parent"
            android:layout_height="0px"
            android:layout_weight="2"
            android:paddingTop="32dp"
            tools:src="@drawable/ic_user_register"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/tv_menu_txt"
            android:layout_width="match_parent"
            android:layout_height="0px"
            android:layout_weight="1"
            android:textColor="@android:color/black"
            android:textSize="22sp"
            android:paddingTop="8dp"
            android:gravity="center_horizontal"
            tools:text="Button Name"/>

    </LinearLayout>

</RelativeLayout>